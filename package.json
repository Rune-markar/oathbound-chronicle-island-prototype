{
  "name": "oathbound-chronicle-island-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/simulation.js && node --test"
  }
}
