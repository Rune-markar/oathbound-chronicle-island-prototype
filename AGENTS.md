# リポジトリ運用

## Git方針

- 実装の変更を伴う場合は worktree を作成して作業する。
- 今回の作業は main へ merge・commit まで行って返す。push はユーザーから明示された場合だけ行う。
- main への merge が成功した場合は、作業 branch と worktree を削除する。

## 仕様変更・更新情報

- 仕様変更・更新情報は、Notion の [LEVIATHAN COVENANT](https://app.notion.com/p/aa8c546868ea4a5887982a6fea1570a4) 配下にある [仕様変更・更新履歴](https://app.notion.com/p/3bd01092fbe2817cbb32d221ab42f6d7) へ必ず記載する。
- 日付、変更の要約、変更理由、影響範囲、実装・検証状況を記載し、関連する仕様ページ、コード、テスト、コミットがある場合は参照を付ける。
- 関連する個別仕様ページ、現状台帳、コード、テスト、リポジトリ内資料も同じ変更単位で更新する。
- `CHANGELOG.md` はリポジトリ内の実装更新履歴として維持するが、Notion への記載を省略する理由にはしない。

## 永続化方針

- 複数マシン間で再利用する価値があり、環境に依存しない事実や確立済み手順は、`AGENTS.md` や `README.md` などリポジトリ内にも残す。

## マシンリソース

- Playwright やブラウザ確認を実施した場合は、応答前に確実に close する。
- コマンドやツールのタイムアウト時は、他セッションによる高負荷の可能性も確認する。
