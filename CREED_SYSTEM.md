# 信条システム

信条は宗教名や性格タグではなく、人物・集団が世界をどう解釈し、誰を仲間と考え、何を正しいと判断するかを表す共通の価値判断レイヤーです。本体は `-100..100` の連続値で、表示名はその要約にすぎません。

## データモデル

実装本体は `src/creed-system.js` です。基本7軸は次のIDで管理します。

- `raceView`: 多種族普遍主義 ↔ 種族選民主義
- `orthodoxy`: 習合・多元主義 ↔ 教義純化・正統主義
- `clericalAuthority`: 地方・個人信仰 ↔ 中央教権主義
- `theocracy`: 世俗主義・政教分離 ↔ 神権主義
- `ritualism`: 神秘・内面的信仰 ↔ 律法・儀礼主義
- `asceticism`: 現世肯定 ↔ 禁欲・来世主義
- `missionary`: 非布教・共同体限定 ↔ 布教・普遍的救済

```js
{
  schemaVersion: 1,
  axes: {
    raceView: { value: -65, importance: 0.8 },
    // 新しい軸も同じ形式で追加可能
  },
  flexibility: 0.65,
  identity: { formed, name, shortDescription, doctrines, tendencies },
  dominantCreedTraits: ["multi_racial_universalist"],
  history: [],
}
```

`value` と `importance` は別です。同じ `value: 80` でも、重要度が低ければAI補正や人物関係への影響は小さくなります。連続値は常に範囲内へ正規化され、追加軸は破棄されません。

## 人物・集団・制度

人物は `state.officers[id].creed`、プレイヤーは `state.playerCreed` に完全なプロファイルを持ちます。

都市は一つの平均値にせず、`state.cities[id].creed` の下で次を分離します。

- `social`: 一般社会の価値観
- `ruling`: 支配層の価値観
- `institutional`: 法・教育・公認組織に固定された価値観

都市派閥もそれぞれ `creed` を持ちます。国家信条は人口だけでなく、政治権力、法的権威、教育、文化的慣性を別の重みとして都市から集約します。社会と制度の差は `tension` として保持され、政策反動の原因になります。

## AI評価

思想的意味を持つ行動だけに `creedEffects` を付けます。

```js
{
  id: "appoint_other_race_general",
  creedEffects: [
    { id: "raceView", direction: -0.8, relevance: 0.9 },
  ],
}
```

`evaluateCreed()` は、方向 × 重要度 × 関連度 × 社会的争点化度から加算用の補正を返します。`campaign-system.js` では既存の人物利害 `interestDisposition` に `creedModifier` を加えます。信条が忠誠・野心・人間関係を置き換えることはありません。

人物関係は、思想差だけでなく双方の重要度と争点化度を通します。関心が低い軸の相違はほぼ関係を動かしません。

## 経験と慣性

事件は `creedImpact` を持てます。

```js
{
  id: "saved_by_other_race",
  creedImpact: [{ id: "raceView", delta: -6 }],
}
```

`applyCreedImpact()` は柔軟性、関与度、性格補正、現在の信念の強さ、端点への飽和を考慮します。通常事件は小さく積み重なり、強い信念ほど逆方向へ動きにくくなります。重要な変化だけが `history` に残ります。

現在は、難民、不作、疫病などの選択、思想タグ付き命令、政策決定が人物・プレイヤー・都市の信条経験へ接続されています。月次には制度と支配層から社会へごく小さな伝播だけが起こり、政策一回で社会全体が反転することはありません。

## 信条名

次のいずれかを満たす場合だけ明示的な信条名を作ります。

- 主要軸が1つでも絶対値80以上
- 複数軸が絶対値60以上

名称は強い軸の語彙を規則で合成します。教義文は2～5件生成しますが、AIが読むのは文章ではなく元の連続値です。`worldTerm` を渡すと神、地名、聖地などを名称へ組み込めます。

## 政策支持と反動

`getPolicyCreedSupport()` は次を別々に返します。

- 社会・支配層・制度の支持
- 各派閥の物質的利害と信条補正
- 現地人物の信条補正
- 支持派、反対派、社会内部の分裂

政策実施時は一律の「政策コスト+20%」にはせず、派閥支持、過激化、人物忠誠、政策論争、妥協要求、施行遅延、地方拒否という政治状態へ変換します。制度値は先に変わりますが、社会意識は月次の遅い伝播でしか変わりません。

## 宗教との分離

宗教は神、神話、聖地、儀式、組織を持つ別オブジェクトです。`createReligiousInterpretation(religionId, creed)` は同じ宗教に複数の信条解釈を結び付けます。

`deriveCreedFactionPressure()` は制度信条から長期間離れた人物・集団を既存派閥へ接続するための圧力を返します。`evaluateReligiousSchism()` は同一宗教内の解釈差、継続期間、政治対立、組織的独立性から分派リスクを返します。正統派・改革派などの固定テンプレートは作りません。

## UIと負荷

人物画面は生成名、主要傾向、詳細7軸、主要教義を表示します。都市の派閥画面は社会・支配層・制度の差と制度緊張を表示し、政策選択肢にはその都市での信条支持を表示します。

AI向けには `dominantCreedTraits` をキャッシュしますが、元の連続値は保持します。一般NPCを追加する場合は、集団信条を基準に小さな個人偏差だけを持たせ、重要人物だけ完全な履歴を持たせてください。

## 拡張手順

1. 新しい軸は `CREED_AXIS_DEFINITIONS` に表示語彙と両側の意味を追加する。
2. 思想的意味がある行動だけへ `creedEffects` を追加する。
3. 歴史経験へ小さな `creedImpact` を追加する。
4. 既存AIの総合点へ `evaluateCreed()` の結果を一項目として加える。
5. 新しい集団は `createCreedGroup()` または `aggregateCreedGroup()` で三層を保持する。
6. 宗教オブジェクトへ信条を埋め込まず、解釈として関連付ける。

世代交代、制度化、公称信条と行動実績の乖離、世界生成時の長期歴史からの逆算は、このデータ構造と履歴APIの上へ追加できます。
