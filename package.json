{
  "name": "oathbound-chronicle-continental-prototype",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check-source.mjs && node --test --test-concurrency=4"
  }
}
