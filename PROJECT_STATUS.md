# 現状台帳の更新規約

ゲーム開始画面の「現状台帳を確認」から開く台帳を、実装状態の正本として管理する。表示データは `src/project-status-data.js` に置く。

機能単位の未実装、部分接続、後続優先度、受入条件、明示的な対象外は [`UNIMPLEMENTED_FEATURES.md`](./UNIMPLEMENTED_FEATURES.md) にまとめる。現状台帳の分類を要約する補助正本であり、仕様変更時は両方を同時更新する。

## 仕様変更時の必須更新

仕様、導入範囲、廃止方針、通常導線、保存形式を変更する作業では、同じ変更内で次を行う。

1. Notion の [仕様変更・更新履歴](https://app.notion.com/p/3bd01092fbe2817cbb32d221ab42f6d7) に日付、要約、理由、影響範囲、実装・検証状況を記載する。
2. 対応する台帳項目の `category`、`summary`、`evidence`、`updatedAt` を更新する。
3. 新機能は台帳項目を追加し、実装ファイルの公開APIまたは定義名と回帰テストを出典にする。
4. 設定や資料だけを追加した場合は `configured`、独立システムを通常ループへ接続していない場合は `unintroduced` とする。
5. 実装根拠がない計画は `planned` とし、「実装済」と混在させない。
6. 資料と現行コードが一致しない場合は削除せず `conflict` として記録し、双方の出典を付ける。
7. `STATUS_LEDGER_META.lastAuditedAt` と監査範囲を更新する。

## 出典の基準

- ローカル出典はファイルパスだけで終わらせず、公開API、定数、章、テスト対象などの `ref` を付ける。
- 通常導線を根拠に `implemented` とする場合は、状態を扱う公開APIだけでなく、その機能を表示・操作するUIの関数または要素と、通常導線の回帰テストも出典に含める。
- 過去の指示が仕様判断に影響する場合はCodexのthread IDを記録する。
- 履歴上「実装した」と書かれていても、現行ソースまたはテストで確認できない場合は実装済みに分類しない。
- 行番号は編集でずれるため補助情報とし、追跡の主キーにはAPI名、定数名、見出し名、テスト名を使う。

## 分類の意味

- `implemented`: 通常導線または共有APIへ導入済み。
- `configured`: 設定、資料、画像、限定UIまで。全体導入ではない。
- `unintroduced`: システム単体は存在するが正規ループや生成世界へ未接続。
- `planned`: 設計のみ、または明示的な未実装。
- `conflict`: 出典間に不一致があり判断・更新が必要。
- `reference`: 現行仕様を追う資料や履歴監査の入口。

## 検証

`tests/project-status.test.mjs` は分類、日付、出典、ローカル参照先、ID重複を検査する。通常の `npm run check` に含まれる。

# 2026-08-18 魔法・技能探究システム

- 実装済み: 魔法12種、常時技能13種、複合・代替取得条件、進捗表示、4+3装備枠、保存移行。
- 戦闘接続済み: 主人公の装備魔法だけを個人戦で選択・対象指定でき、知力由来の魔力、装備技能補正、魔法使用回数、被害、勝敗、瀕死勝利を次の取得進捗へ反映する。
- 非戦闘接続済み: 地方・個人移動、探索、ダンジョン踏破、戦利品、治療、NPC会話、仲間加入、依頼達成、窃盗・密輸成功を取得進捗へ反映する。
- 明確な境界: 今回の25種は主人公の個人戦用。国家規模の部隊魔術研究、NPCごとの独立した習得台帳、魔法を使うフィールド謎解きは未実装。

# 2026-08-19 全国家軍ユニット生成

- 実装済み: 現行生成世界の7文化すべてに軍制と兵科アーキタイプを定義し、通常軍務へ出る全国家ユニットを国家・任務・陣営・地形・方針・シードから決定論的に生成。部隊名、出身、軍旗、訓練度、地形順応、特性、経験、補正、魔法構成、生成フィンガープリントを戦闘状態へ保持する。
- 通常導線接続済み: 従士・部隊長の軍務が発令国と作戦地方を読み、部隊・指揮官・地形・両国名を通常戦術UIへ渡す。固定5対5開発戦闘は検証入口として残すが、通常軍務の正本には使わない。
- 検証済み: 同一任務の完全再現、別国家・任務・地形での差異、420生成個体・4訓練度・4地形順応・8兵科の分布、7文化・4地形・左右入替の168戦。全文化に勝利と敗北があり、生成差を含めても文化別勝率15〜80%、膠着15%未満。
- 明確な境界: 国家単位の月次戦争台帳と、20×14の個別軍務戦場は状態と損害を共有する全面接続には至っていない。今回の対象は通常軍務で使う国家編成と戦術個性である。
